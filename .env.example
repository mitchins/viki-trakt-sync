# Environment variables for viki-trakt-sync

# Viki credentials
VIKI_USER=your@email.com
VIKI_PASS=yourpassword

# Get token by logging in via browser:
# 1. Login at https://www.viki.com
# 2. Open DevTools → Network tab
# 3. Find any api.viki.io request
# 4. Copy token parameter from URL (starts with 'ex1')
VIKI_TOKEN=

# Trakt API credentials (get from https://trakt.tv/oauth/applications)
TRAKT_CLIENT_ID=your_client_id
TRAKT_CLIENT_SECRET=your_client_secret

# Optional: Pushover notifications for token expiry
# Get these at https://pushover.net (free account)
# 1. Create free account
# 2. Create app in dashboard
# 3. Copy User Key and App Token below
# Notifications are only sent on FIRST token failure, not repeated runs
PUSHOVER_USER_KEY=
PUSHOVER_APP_TOKEN=

# Optional: Override paths
CONFIG_DIR=~/.config/viki-trakt-sync
TOKEN_STATUS_PATH=~/.config/viki-trakt-sync/token_status.json

