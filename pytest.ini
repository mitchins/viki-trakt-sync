[pytest]
# Suppress resource warnings for unclosed database connections
# These are from internal sqlite3 instances that get cleaned up by GC
filterwarnings =
    ignore::ResourceWarning:sqlite3
testpaths = tests
